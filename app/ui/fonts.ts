import { Inter, Lusitana } from 'next/font/google'

export const inter = Inter({
  subsets: ['latin'],
})

export const lusitana = Lusitana({
  subsets: ['latin'],
  weight: '400', // specify the font weight here, e.g., '400' for normal weight or '700' for bold
})
